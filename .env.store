# Store / public build (used via `vite build --mode store`, i.e. `npm run package`).
# No Google Photos client id → the Google Photos destination is hidden and its
# optional permissions are omitted from the manifest. Overrides .env for store
# builds only; your local `.env` still enables Photos for personal testing.
STEGOSHARD_GOOGLE_CLIENT_ID=
